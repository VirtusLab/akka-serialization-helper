akka.actor {
  serializers {
    borer-json = "benchmark.borer.TestBorerJsonAkkaSerializer"
  }
  serialization-bindings {
    "benchmark.model.MySerializable" = borer-json
  }
  enable-additional-serialization-bindings = on
}
