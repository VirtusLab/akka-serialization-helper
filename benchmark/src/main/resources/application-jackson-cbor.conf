akka{
    actor {
      serialization-bindings {
        "benchmark.model.MySerializable" = jackson-cbor
      }
        enable-additional-serialization-bindings = on
    }
    serialization{
        jackson-modules += "com.fasterxml.jackson.module.scala.DefaultScalaModule"
    }

}
